# -*- coding: utf-8 -*-
"""Estructuras de almacenamiento de datos - POO

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1Sg9CBgCqVwdXmZRR3AoNzRleoEHQddxE
"""

"""
tomar los metodos que estan en el pdf y usarlos para hacer un crud, deben hacer
una base de datos estructurada con array dentro del array deben tener solo la
informacion del cliente (solo el nombre)

en el codigo principal deben hacer un menu de trabajo como interfaz de usuario
con las 9 opciones que tiene el pdf :

1. guardar un cliente
2. guardar varios clientes
3. insertar cliente en posicion determinada
4. eliminar cliente
5. buscar cliente y eliminar cliente
6. buscar cliente e imprimir el cliente
7. buscar cliente
8. contar las veces que se repite el cliente
9. ordenar lista de menor a mayor
10. ordenar lista de mayor a menor
11. ver la base de datos
12. salir del sistema
"""
#zona de clases
class Lista_cliente:
  #crear un constructor --> construir el objetocon caracteristicas iniciales
  def __init__(self):
    #creo los atributos
    self.base_datos = ["zoe", "sebas"]
    print("la base de datos se creo en blanco")

  #creo los metodos con las responsabilidades

  def guardar_info(self, nombre_cliente):
    self.base_datos.append(nombre_cliente)

  def ver_info(self):
    print(type(self.base_datos))
    print(self.base_datos)
    #mostrar solo la informacio del array
    # primer (for index)
    for i in range(len(self.base_datos)):
      print(f"nombre del cliente es: {self.base_datos[i]}")
    # segundo for (por objetos internos)
    for nombre_cliente in self.base_datos:
      print(f"nombre del cliente es: {nombre_cliente}")

#zona de codigo principal

obj_db = Lista_cliente()
info_cliente = input("escriba el nombre: ")
obj_db.guardar_info(info_cliente)
obj_db.ver_info()

# zona de clases

class Lista_cliente:
  def __init__(self):
    self.base_datos = []
    self.varios = []

  def guardar_opcion(self):
      while True:
        print("-----MENU-----")
        print("1. guardar un cliente ")
        print("2. guardar varios clientes")
        print("3. insertar cliente en posicion determinada")
        print("4. eliminar cliente")
        print("5. buscar cliente y eliminarlo")
        print("6. buscar cliente e imprimir")
        print("7. buscar cliente")
        print("8. contar las veces en que se repite el cliente")
        print("9. ordenar lista de menor a mayor")
        print("10. ordenar lista de mayor a menor")
        print("11. ver la base de datos")
        print("12. salir del sistema")
        cont = int(input("digite la opcion:"))

        if cont == 1:
          nombre_cliente = input("digite el nombre del cliente: ")
          self.base_datos.append(nombre_cliente)

        if cont == 2:
          guardar_clientes = int(input("digite la cantidad de clientes: "))
          for d in range(0,guardar_clientes):
            base_datos = input("digite el nombre de cliente: ")
            self.varios.append(base_datos)
          self.base_datos.extend(self.varios)

        if cont == 3:

          posicion = int(input("digite la posicion: "))
          nombre_cliente = input("digite el nombre del cliente: ")
          self.base_datos.insert(posicion, nombre_cliente)

        if cont == 4:
          nombre_cliente = input("escriba el nombre que desea eliminar: ")
          self.base_datos.remove(nombre_cliente)

        if cont == 5:
          nombre_cliente = input("digite el nombre del cliente que desea buscar e eliminar: ")
          posicion = self.base_datos.index(nombre_cliente)
          self.base_datos.pop(posicion)

        if cont == 6:
          nombre_cliente = input("digite el nombre del cliente: ")
          posicion = self.base_datos.index(nombre_cliente)
          print(f"el cliente esta en la posicion : {posicion}")

        if cont == 7:
          nombre_cliente = input("digite el nombre del cliente a buscar: ")
          posicion = self.base_datos.index(nombre_cliente)
          print(f"el cliente se encuentra en la posicion: {posicion}")

        if cont == 8:
          nombre_cliente = input("digite el nombre del cliente a buscar: ")
          veces_repetidas = self.base_datos.count(nombre_cliente)
          print(f"el cliente se repite {veces_repetidas} veces")

        if cont == 9:
          self.base_datos.sort()
          print(f"la lista ordenada de menor a mayor: {self.base_datos}")

        if cont == 10:
          self.base_datos.sort()
          self.base_datos.reverse()
          print(f"la lista ordenada de mayor a menor: {self.base_datos}")

        if cont ==11:
          for guardar_clientes in range(len(self.base_datos)):
            print(f"nombre del cliente es: {self.base_datos[guardar_clientes]}")
            guardar_clientes == 0

        if cont == 12:
          print("salir del sistema")
          break

# zona de codigo principal

obj_db = Lista_cliente()
obj_db.guardar_opcion()
